# TODO - Fix code + run virtual system (CLI) 

- [x] Inspect repository code: read `inventory_system/app.py`, `inventory_system/cli.py`, `inventory_system/external_api.py` and unit tests.
- [x] Inspect project entrypoints and configuration: read `run.py`, `requirements.txt`, `README.md`.
- [x] Run unit tests to identify failing parts.
- [x] Fix failing code so all tests pass.
- [x] Create/refresh a project-local virtual environment and install dependencies.
- [x] Run `pytest` in the venv.
- [x] Start Flask API server in the background.
- [x] Run the CLI commands (at least `list` and `search`) against the running server.



